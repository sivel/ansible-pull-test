---
- hosts: localhost
  gather_facts: false
  tasks:
    - raw: python -c 'import os; print s.path.expanduser(os.path.expandvars("~"))'
    - raw: 'echo ~'
    - template:
        src: template.j2
        dest: /tmp/template.out
